# MongoDB Atlas Connection (REQUIRED - Replace with your Atlas connection string)
MONGODB_URI=mongodb+srv://intellispec_dev_user:PKTDEVD8@intellispec-dev-az-east.rnkdpz6.mongodb.net/?retryWrites=true&w=majority&appName=intellispec-dev-az-eastus2

# JWT Secret (REQUIRED - Replace with your own secure secret)
JWT_SECRET=super-secret-jwt-key-for-development-change-in-production-32-chars-min

# OpenAI API Key (Optional - only needed for AI features)
OPENAI_API_KEY=your-openai-api-key-here

# Application Settings
NODE_ENV=development
PORT=4000
ENABLE_AI_RAG=true
ENABLE_VECTOR_SERVICE=false

# Security Settings
BCRYPT_SALT_ROUNDS=12
MAX_LOGIN_ATTEMPTS=5
ACCOUNT_LOCKOUT_MINUTES=15
ENFORCE_AUTH=false

REACT_APP_OPENAI_API_KEY=your-openai-api-key-here
# Optional OpenAI Configuration
REACT_APP_OPENAI_BASE_URL=https://api.openai.com/v1
REACT_APP_OPENAI_TIMEOUT=30000
REACT_APP_OPENAI_RETRIES=3

REACT_APP_API_BASE=http://localhost:4000

DB_MAX_POOL_SIZE=50
DB_MIN_POOL_SIZE=5
DB_CONNECT_TIMEOUT=10000
DB_SERVER_SELECTION_TIMEOUT=5000
DB_SOCKET_TIMEOUT=45000
DB_MAX_IDLE_TIME=60000
DB_WAIT_QUEUE_TIMEOUT=5000
DB_MAX_RETRY_ATTEMPTS=5
DB_MONITOR_INTERVAL=60000
# Rate Limiting (High limits for development)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=10000
LOGIN_RATE_LIMIT_MAX=1000
LOGIN_RATE_LIMIT_WINDOW_MS=60000

# Vector Update Service (safe defaults)
VECTOR_ALLOWED_COLLECTIONS=documents
VECTOR_DISCOVERY_ENABLED=false
VECTOR_MAX_COLLECTIONS=3
VECTOR_BATCH_SIZE=5
VECTOR_RATE_LIMIT_DELAY=500
VECTOR_RETRY_DELAY=10000
VECTOR_DEBOUNCE_MS=3000
VECTOR_MAX_RETRIES=3
VECTOR_DISCOVERY_SAMPLE_LIMIT=100

# Password reset + email delivery
# Set APP_BASE_URL to the public host serving the React app (include protocol)
APP_BASE_URL=http://localhost:4001
# Or specify FRONTEND_PORT to auto-generate http://localhost:<port> when APP_BASE_URL is empty
FRONTEND_PORT=4001
PASSWORD_RESET_PATH=/reset-password
PASSWORD_RESET_TOKEN_TTL_MINUTES=60
PASSWORD_RESET_SUBJECT=Reset your intelliSPEC password
PASSWORD_RESET_FROM=notifications@intellispec.local

EMAIL_TRANSPORT=console # console | smtp | disable
SMTP_URI=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false
